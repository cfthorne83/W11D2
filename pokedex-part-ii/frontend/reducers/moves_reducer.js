

const movesReducer = (state = {}, action) => {
    Object.freeze(state);
    let nextState = Object.assign({}, state);

    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}

export default movesReducer;